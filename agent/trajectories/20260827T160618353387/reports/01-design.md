I designed a minimal YCSB-on-PostgreSQL experiment, but validation failed because the two systems entries both named "PostgreSQL," which the harness collapses into a single configuration. To satisfy the discriminates requirement, I would need to introduce a second, distinct system (e.g., PgDuckDB) or vary a resource factor (cpu/memory) instead. With no remaining validate attempts, I cannot resolve this here.
