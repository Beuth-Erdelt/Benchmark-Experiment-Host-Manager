I designed a simple YCSB experiment targeting PostgreSQL, but the catalog shows that the ycsb workload only supports PostgreSQL, not PgDuckDB. My last attempt added PgDuckDB to the systems list, which caused validation to fail because the workload does not support that system. To satisfy the discriminates check, I should keep only PostgreSQL in the systems list and remove PgDuckDB. The experiment would then vary only the system factor (PostgreSQL) and meet the catalog's constraints. However, with no more validate attempts allowed, I cannot confirm the corrected file. The unresolved issue is the mismatch between the discriminates field and the actual varied systems due to the catalog's supported workloads list.
