Summary:
I designed an experiment to choose a database system for a 1 GB analytical reporting workload under 16 CPU cores and 64 GiB RAM, and to determine whether the recommendation remains valid under concurrent analyst load. The experiment compares PostgreSQL and PgDuckDB using the TPC-H analytical benchmark with a 1 GB dataset. It isolates two factors: the database system (PostgreSQL vs. PgDuckDB) and concurrency (1, 2, and 4 concurrent analysts). The SUT is pinned to a 64-core node with 64 GiB RAM and node-local storage, while benchmarker pods run on a larger node to avoid resource contention. The experiment runs three repetitions per configuration to ensure statistical reliability. The design is valid per the catalog and cluster environment, and the experiment has been submitted under code 1787829906.
