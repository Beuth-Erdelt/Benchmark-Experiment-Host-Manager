Our service keeps about a gigabyte of user records in PostgreSQL and touches them one row at a time by primary key, with a roughly even mix of reads and updates. As we add concurrent clients, how far does the throughput we get keep rising, and where does response time start to suffer? Choose the workload from the catalog that actually reproduces this access pattern, and keep the run small enough to finish in about an hour.
