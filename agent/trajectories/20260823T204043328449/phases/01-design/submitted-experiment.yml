mode: run
title: "TPC-H SF1 analytical system selection: PostgreSQL vs PgDuckDB, single vs concurrent analysts"
hypothesis: >-
  For a 1 GB TPC-H analytical reporting workload under a hard ceiling of 16 CPU
  cores and 64 GiB RAM, PgDuckDB with its DuckDB execution engine forced
  (duckdb_force_execution=true) will achieve lower per-query latency and higher
  Power@Size throughput than stock PostgreSQL, given identical heap tables,
  statistics, and resource limits. This experiment isolates the system factor
  (PostgreSQL vs PgDuckDB) and the concurrency factor (1 vs 2 parallel analyst
  streams) to determine which system to deploy and whether the single-analyst
  ranking persists when multiple analysts run reports concurrently.
discriminates:
  - system
  - concurrency
workload:
  name: tpch
  params:
    scaling_factor: 1
    timeout: 300
    query_repeats: 2
    verify_result: true
  rounds: [1, 2]
  repetitions: 3
loading:
  pods: 1
  threads: 1
  split: 1
  post_load:
    indexes: false
    constraints: false
    statistics: true
    storage_format: heap
systems:
  - name: PostgreSQL
    profile: analytical-ssd
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
observe:
  monitoring_sut: true
placement:
  sut: cl-worker36
  loading: cl-worker36
  benchmarking: cl-worker36
resources:
  cpu:
    request: 16
    limit: 16
  memory:
    request: 64Gi
    limit: 64Gi
  storage:
    size: 10Gi
