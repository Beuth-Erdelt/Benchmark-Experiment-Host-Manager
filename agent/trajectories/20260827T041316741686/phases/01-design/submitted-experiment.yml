mode: run
title: "TPC-H SF3 PostgreSQL vs PgDuckDB, 16 CPU / 64 GiB, 1-2 streams"
hypothesis: >
  At TPC-H scale factor 3 with identical 16 CPU / 64 GiB SUT limits,
  PgDuckDB with forced DuckDB execution (duckdb_force_execution=true)
  beats PostgreSQL on full-workload analytical performance (per-phase
  Geo Times and Throughput@Size), and the system ranking is stable
  between 1 and 2 concurrent query streams.
discriminates: [system, concurrency]
workload:
  name: tpch
  params:
    scaling_factor: 3
  rounds: [1, 2]
  repetitions: 3
loading:
  pods: 1
  threads: 1
  split: 1
  timeout_minutes: 30
systems:
  - name: PostgreSQL
    profile: analytical-ssd
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
placement:
  sut: cl-worker21
  loading: cl-worker21
  benchmarking: cl-worker21
resources:
  cpu:
    request: 16
    limit: 16
  memory:
    request: 64Gi
    limit: 64Gi
  storage:
    size: 20Gi
