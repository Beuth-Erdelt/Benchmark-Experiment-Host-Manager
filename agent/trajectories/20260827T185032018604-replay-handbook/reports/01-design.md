I designed a minimal, valid YCSB experiment against PostgreSQL to answer the request for a quick and simple example. The specification runs YCSB workload b (read-mostly, single-row primary-key operations) on PostgreSQL with a 1 GB dataset, varying concurrency across two levels (1 and 2 clients) to satisfy the required factor declared in discriminates. It uses a modest, fixed resource envelope (2 CPU cores, 4 GiB memory) to ensure parity and avoid elastic allocation, and it repeats each configuration twice to allow a basic comparison of throughput and latency. The experiment is pinned to a single worker node (cl-worker21) to reduce placement variance. This design is a measurement of PostgreSQL's basic YCSB capability under a minimal setup, not a comparison against another system.
