mode: run
title: "TPC-H SF1 system choice under 16c/64GiB: concurrency sweep 1/2/4 analysts"
hypothesis: >
  Under a 16-core / 64 GiB budget, PostgreSQL's lead over PgDuckDB with forced
  DuckDB execution on a 1 GB TPC-H analytical workload (higher Throughput@Size,
  lower Geo Times) persists at 4 concurrent analysts -- where PostgreSQL's
  parallel-worker demand (2 per gather x 4 queries = 8) exceeds the
  analytical-ssd profile's max_parallel_workers=4 -- preserving the system
  ranking; if the lead narrows materially or the ranking flips at 4 analysts,
  the PostgreSQL recommendation is valid only up to 2 concurrent analysts.
discriminates: [system, concurrency]
workload:
  name: tpch
  params:
    scaling_factor: 1
    timeout: 300
    query_repeats: 2
  rounds: [1, 2, 4]
  repetitions: 3
loading:
  pods: 2
  threads: 1
  post_load:
    statistics: true
systems:
  - name: PostgreSQL
    profile: analytical-ssd
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
observe:
  monitoring_sut: true
  monitoring_cluster: false
  monitoring_app: false
placement:
  sut: cl-worker21
  loading: cl-worker17
  benchmarking: cl-worker19
resources:
  cpu:
    request: 16
    limit: 16
  memory:
    request: 64Gi
    limit: 64Gi
  storage:
    size: 10Gi
