mode: run
title: "pg_duckdb vs PostgreSQL on TPC-H joins under concurrency: memory vs thread contention"
hypothesis: >
  With DuckDB execution forced, PgDuckDB matches or beats PostgreSQL on
  multi-way TPC-H joins as concurrent streams scale from 1 to 8. Any
  throughput degradation as concurrency rises is attributable to per-stream
  memory starvation (degradation markedly worse at a 32Gi memory limit than
  at 64Gi) rather than thread/CPU contention (degradation similar at both
  memory limits).
discriminates: [system, concurrency, memory]
workload:
  name: tpch
  params:
    scaling_factor: 1
    active_queries: [5, 7, 8, 9, 21]
    query_repeats: 10
    timeout: 300
  rounds: [1, 2, 4, 8]
  repetitions: 3
loading:
  pods: 1
  threads: 1
  split: 1
  post_load:
    statistics: true
systems:
  - name: PostgreSQL
    profile: analytical-ssd
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
observe:
  monitoring_sut: true
  monitoring_cluster: true
placement:
  sut: cl-worker36
  loading: cl-worker36
  benchmarking: cl-worker36
resources:
  cpu:
    request: "16"
    limit: "16"
  memory:
    - request: "32Gi"
      limit: "32Gi"
    - request: "64Gi"
      limit: "64Gi"
  storage:
    size: "10Gi"
