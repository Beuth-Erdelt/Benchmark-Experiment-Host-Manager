mode: run
title: "PgDuckDB default cost-based routing on TPC-H SF1 at 1 and 8 concurrent streams"
hypothesis: >-
  Leaving pg_duckdb's cost-based routing at its default
  (duckdb_force_execution=false) changes latency and throughput relative to
  forcing every query through the DuckDB executor. This experiment measures
  the default-routing side on a 1 GB TPC-H database with 16 CPU cores and
  64 GiB RAM at 1 and 8 concurrent streams across all 22 queries, using the
  identical design to the completed forced-execution experiment
  (duckdb_force_execution=true); the two are compared overall and per query
  shape to decide whether to force execution or leave routing at default.
discriminates:
  - concurrency
workload:
  name: tpch
  params:
    scaling_factor: 1
    query_repeats: 2
  rounds: [1, 8]
  repetitions: 3
loading:
  pods: 1
  threads: 1
  post_load:
    statistics: true
systems:
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: false
placement:
  sut: cl-worker36
  loading: cl-worker36
  benchmarking: cl-worker36
resources:
  cpu:
    request: 16
    limit: 16
  memory:
    request: 64Gi
    limit: 64Gi
  storage:
    size: 10Gi
