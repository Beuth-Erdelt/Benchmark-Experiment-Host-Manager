I designed an experiment to choose between PostgreSQL and PgDuckDB for a 1 GB TPC-H analytical workload under a 16 CPU / 64 GiB budget and to test whether any advantage persists under concurrent analysts. The specification pinned the SUT to cl-worker21, set CPU and memory limits, used local-hdd storage, and swept concurrency with rounds [1,4,8] across three repetitions. I corrected the active_queries list and initially set storage_class to local-hdd, but the analytical-ssd profile required ssd or null; switching to ssd failed because the environment only lists local-hdd as a non-ceph, node-local option. I could not resolve the storage class mismatch within the remaining validation budget, so the experiment remains unvalidated.
