I designed an experiment to choose between PostgreSQL and PgDuckDB for a 1 GB analytical reporting workload under a 16 CPU / 64 GiB RAM ceiling, and to test whether the recommendation holds under 4 concurrent analysts. The specification isolates the system choice and concurrency level as the only varying factors, applies identical physical design (indexes, constraints, statistics) to both systems, and sets repetitions to 3 for statistical robustness. It pins the SUT, loaders, and benchmarker to cl-worker21, which provides 64 allocatable CPUs and ~593 GiB RAM, and uses a 120 Gi storage volume with the local-hdd storage class.

The unresolved issue is that the benchmarker’s declared peak CPU usage (80 cores at round 4) exceeds cl-worker21’s allocatable 64 cores, so placement must be adjusted to either lower the rounds list or move the benchmarker to a larger node.
