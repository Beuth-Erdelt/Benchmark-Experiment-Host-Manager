I designed an experiment that compares PostgreSQL at 4 vs 8 CPU cores on a 1 GB YCSB workload A (50/50 read/update) to measure which serves more requests per second and by how much. The specification uses the YCSB workload from the catalog, pins everything to the same node and storage class, and sets repetitions=3 to estimate variation. The only thing unresolved is that the catalog currently forbids resource sweeps for YCSB, so the CPU levels must be run as two separate experiment runs rather than one sweep.
