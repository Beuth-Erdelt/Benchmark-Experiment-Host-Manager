mode: run
title: "pg_duckdb 8-stream join degradation: direct RSS/CPU trace to confirm memory starvation vs thread contention"
hypothesis: >
  With DuckDB execution forced at 8 concurrent streams on the SF=1
  join-heavy subset (Q5, Q7, Q8, Q9, Q21), the throughput degradation is
  attributable to per-stream memory starvation -- the SUT's RSS approaches
  the 32Gi memory limit at the 32Gi cell, with memory-related cancellations
  recurring only there, while RSS stays well below the limit at the 64Gi
  cell -- rather than to thread/CPU contention, which would show
  near-saturated CPU with RSS well below the limit at both memory cells.
discriminates: [memory]
workload:
  name: tpch
  params:
    scaling_factor: 1
    active_queries: [5, 7, 8, 9, 21]
    query_repeats: 40
    timeout: 300
  rounds: [8]
  repetitions: 3
loading:
  pods: 1
  threads: 1
  split: 1
  post_load:
    statistics: true
systems:
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
observe:
  monitoring_sut: true
  monitoring_cluster: true
placement:
  sut: cl-worker36
  loading: cl-worker36
  benchmarking: cl-worker36
resources:
  cpu:
    request: "16"
    limit: "16"
  memory:
    - request: "32Gi"
      limit: "32Gi"
    - request: "64Gi"
      limit: "64Gi"
  storage:
    size: "10Gi"
