I designed a comparison between plain PostgreSQL and PgDuckDB (with DuckDB execution forced) on 22 analytical queries over ~10 GB of TPC-H data. The specification sets repetitions=3, rounds=[1], and pins both systems to the same node and ssd-like storage. The first validation failed because the analytical-ssd profile requires storage_class to be ssd or null, but the cluster only offers ssd via the cephcsi storage class, not ssd. The second validation failed because the environment does not have a storage class named ssd; it only has cephcsi, which is the closest ssd-like option. I did not have remaining validate calls to try cephcsi or to omit storage_class and rely on node-local storage, so the specification remains unresolved on the storage class choice.
