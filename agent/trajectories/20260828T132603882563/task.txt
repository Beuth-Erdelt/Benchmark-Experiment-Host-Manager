Our reporting team runs the same two dozen analytical queries over a few gigabytes of sales history every morning, and they say it takes too long. Someone has suggested swapping in a version of PostgreSQL that has a vectorised query engine attached, meaning one that processes rows in batches rather than one at a time. Is that actually faster for this kind of query, and by how much? Set up a fair comparison between plain PostgreSQL and the vectorised variant, and keep the run small enough to finish in about an hour.
