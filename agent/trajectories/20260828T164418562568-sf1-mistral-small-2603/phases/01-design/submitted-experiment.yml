mode: run
title: "Isolate CPU vs memory pressure for analytical workload (TPC-H SF1)"
hypothesis: "The nightly reporting job's performance depends on either CPU cores or memory. Halving CPU from 16 to 8 cores or halving memory from 64 GiB to 32 GiB will each cause a measurable slowdown in query latency; the experiment will determine which resource is the bottleneck and whether either cut is safe on its own."
discriminates: [cpu, memory]

workload:
  name: tpch
  params:
    scaling_factor: 1
    timeout: 1200
    query_repeats: 1
    active_queries: [5, 7, 8, 9, 21]
    recreate_parameter: false
    shuffle_queries: false
    refresh_streams: 0
    store_explain: false
  rounds: [2]
  repetitions: 3

loading:
  pods: 1
  threads: 1
  split: 1
  timeout_minutes: 30
  post_load:
    indexes: true
    constraints: true
    statistics: true
    storage_format: heap

systems:
  - name: PostgreSQL
    profile: analytical-ssd

resources:
  cpu:
    - request: "16"
      limit: "16"
    - request: "8"
      limit: "8"
  memory:
    - request: "64Gi"
      limit: "64Gi"
    - request: "32Gi"
      limit: "32Gi"
  storage:
    size: "100Gi"
  storage_class: null

placement:
  sut: cl-worker36

observe:
  monitoring_sut: true
  monitoring_cluster: false
  monitoring_app: false

max_sut: 1
max_sut_experiment: 1