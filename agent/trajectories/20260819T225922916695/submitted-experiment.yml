mode: run
title: "pg_duckdb: forced DuckDB execution vs cost-based routing (TPC-H SF=1, 16c/64GiB)"
hypothesis: >
  On a 1 GB TPC-H database with 16 CPU cores and 64 GiB RAM, forcing every
  query through pg_duckdb's DuckDB executor (duckdb_force_execution=true)
  yields different overall throughput and per-query latency than leaving
  pg_duckdb's cost-based routing at its default (duckdb_force_execution=false).
  The experiment determines which choice is better overall and whether the
  winner is consistent across the 22 TPC-H query shapes at 1 and 8 concurrent
  streams, or whether some query shapes favor the opposite choice.
discriminates: [system, concurrency]
workload:
  name: tpch
  params:
    scaling_factor: 1
    timeout: 600
  rounds: [1, 8]
  repetitions: 3
loading:
  pods: 1
  threads: 1
  split: 1
  post_load:
    statistics: true
systems:
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: false
observe:
  monitoring_sut: false
  monitoring_cluster: false
  monitoring_app: false
placement:
  sut: cl-worker36
  loading: cl-worker36
  benchmarking: cl-worker36
resources:
  cpu:
    request: 16
    limit: 16
  memory:
    request: 64Gi
    limit: 64Gi
  storage:
    size: 20G
