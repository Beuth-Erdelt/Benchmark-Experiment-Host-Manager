Using two sequential experiments, and never two same-named system entries in one experiment, determine whether I should force pg_duckdb's DuckDB executor or leave cost-based routing at its default on a 1 GB TPC-H database with 16 CPU cores and 64 GiB RAM. In the initial experiment, benchmark PgDuckDB with duckdb_force_execution=true at 1 and 8 concurrent streams across all 22 queries. After interpreting that result, use the one follow-up experiment for the identical design with duckdb_force_execution=false. I need an overall recommendation and to know whether it is consistent across individual query shapes, or whether some queries favor the opposite mode.
