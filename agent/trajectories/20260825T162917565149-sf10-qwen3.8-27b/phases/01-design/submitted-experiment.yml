mode: run
title: "TPC-H SF10: PostgreSQL vs forced-PgDuckDB under 16 CPU / 64 GiB"
hypothesis: >
  Under a fixed 16 CPU / 64 GiB envelope, PostgreSQL remains the better
  choice than forced-DuckDB PgDuckDB for concurrent analytical reporting:
  across concurrency levels 1, 2, and 4, PostgreSQL achieves lower per-query
  latency and higher Power@Size / Throughput@Size than PgDuckDB running with
  duckdb_force_execution=true. The only factor that varies between the two
  systems is the system itself; workload, resources, physical design, and
  profile are held identical.
discriminates: [system, concurrency]
workload:
  name: tpch
  params:
    scaling_factor: 10
  rounds: [1, 2, 4]
  repetitions: 3
loading:
  pods: 4
  threads: 1
  split: 1
  timeout_minutes: 120
  post_load:
    indexes: false
    constraints: false
    statistics: true
    storage_format: heap
systems:
  - name: PostgreSQL
    profile: analytical-ssd
  - name: PgDuckDB
    profile: analytical-ssd
    override:
      duckdb_force_execution: true
observe:
  monitoring_sut: true
  monitoring_cluster: false
  monitoring_app: false
placement:
  sut: cl-worker28
  loading: cl-worker29
  benchmarking: cl-worker27
resources:
  cpu:
    request: 16
    limit: 16
  memory:
    request: 64Gi
    limit: 64Gi
  storage:
    size: 50Gi
