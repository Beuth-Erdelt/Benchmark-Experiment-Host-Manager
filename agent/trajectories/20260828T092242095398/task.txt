Our service keeps about a gigabyte of user records in PostgreSQL and touches them one row at a time by primary key, with a roughly even mix of reads and updates at a high request rate. We can give that PostgreSQL either 4 or 8 CPU cores; which of the two serves more requests per second, and by how much? Choose the workload from the catalog that actually reproduces this access pattern, and keep the run small enough to finish in about an hour.
